# Hardware report

Captured with `scripts/report_hardware.py`. Re-run and re-append (do not
edit past snapshots) before any phase whose numbers depend on timing.

## Snapshot: 2026-08-24T16:48:58Z (development workstation, session start)

```json
{
  "captured_at_utc": "2026-08-24T16:48:58.293136+00:00",
  "platform": "Linux-6.17.0-14-generic-x86_64-with-glibc2.39",
  "python_version": "3.12.3 (main, Jan 22 2026, 20:57:42) [GCC 13.3.0]",
  "cpu_count_logical": 8,
  "gpus": [],
  "mem": {
    "MemTotal": "7739112 kB",
    "MemAvailable": "3372024 kB",
    "SwapTotal": "2097148 kB"
  },
  "disk_free_gb": 55.7,
  "disk_total_gb": 232.4,
  "torch": {
    "torch_version": "2.13.0+cpu",
    "cuda_available": false,
    "mps_available": false
  }
}
```

## Reading

- **No GPU.** `nvidia-smi` is not installed/present; `torch.cuda.is_available()`
  is `False`. Every experiment in this project runs on CPU until/unless a
  GPU-equipped machine is substituted in, which would need its own snapshot
  here before its numbers are trusted.
- **~3.3 GiB available RAM** out of 7.4 GiB total at capture time (the rest
  is used by the host OS/session). This is the binding constraint on batch
  size and model width, tighter than disk (55.7 GiB free) or CPU count (8).
- Torch 2.13.0 CPU build, Python 3.12.3. Full transitive dependency pins
  are in `requirements.lock.txt` (generated by `pip freeze`, committed for
  reproducibility, regenerate rather than hand-edit).

## Resource-estimate consequences

| Planned workload | Feasible on this snapshot? | Note |
|---|---|---|
| Phase 0 synthetic smoke tests (tiny CNN, <1k synthetic samples) | Yes | Seconds per run |
| Phase 0 two-client FedAvg round (synthetic) | Yes | Seconds |
| Phase 1 full Fed-ISIC2019 (23,247 real images, 224px, ResNet-scale backbone, 5 seeds × 5+ baselines) | **No** — not attempted on this hardware | Would need GPU + hours; flagged PENDING, config prepared, not run |
| Phase 1 small deterministic real-data subset (once license accepted), tiny CNN, few epochs | Plausible, to be timed when the subset exists | Wall-clock recorded in the run's metrics JSON when attempted |
| SCAFFOLD / DP-SGD sweeps / gradient inversion on real data | Deferred until Phase 1 subset timing is known | See `docs/execution_plan.md` |
| Fed-IXI Tiny (444 MB, 3D volumes) | Plausible at small batch/patch size once reached (Phase 7) | Not attempted yet |
| FeTS/BraTS, ADNI, full FHE, multi-node blockchain network | **No** | Explicitly out of scope for this hardware per project brief |
